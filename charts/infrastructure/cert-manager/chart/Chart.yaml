apiVersion: v2
name: cert-manager
description: cert-manager plus a self-signed homelab root CA bootstrap
type: application
version: 0.1.0

dependencies:
  - name: cert-manager
    version: 1.16.2
    repository: https://charts.jetstack.io
